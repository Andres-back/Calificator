# Contratos

No se agregan ni modifican contratos HTTP.

Se reutilizan:

- `GET /evaluaciones/{evaluacion_id}/calificaciones` para conocer estudiantes con calificación existente.
- `POST /calificaciones/foto` para aceptar una evidencia y crear la calificación asíncrona.

