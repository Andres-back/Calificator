# Vite development is localhost-only by default.
# Set this only when you intentionally need to test from another device on your LAN.
# VITE_ALLOW_LAN=true

# Controla la captura experimental en el workspace; el panel administrativo
# consulta además la bandera efectiva y los permisos del backend.
VITE_IMPACT_STUDY_ENABLED=false
